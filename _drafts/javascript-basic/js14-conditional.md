





### 조건문

```javascript
if(a > b && c < d) {
} else if (a >= b || c <= d) {
} else if (!== a) {
}

// 조건부 연산자
(조건식) ? 실행구문1 : 실행구문2;

// switch
switch(val) {
  case 1:
    //...
    break;
  case 2:
    //...
    break;
  default:
    //...
    break;
}
```

